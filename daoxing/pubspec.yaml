name: daoxing
description: "道行 - 基于运动的修仙游戏"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 状态管理
  flutter_riverpod: ^2.6.1

  # 本地存储
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  shared_preferences: ^2.3.4

  # 健康数据
  health: ^11.1.1
  pedometer: ^4.2.0

  # 定位与天气
  geolocator: ^13.0.2
  http: ^1.2.2

  # 地图
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

  # UI 增强
  flutter_animate: ^4.5.2
  percent_indicator: ^4.2.3
  fl_chart: ^0.69.0

  # 工具
  intl: ^0.19.0
  permission_handler: ^11.4.0
  flutter_local_notifications: ^18.0.1
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
